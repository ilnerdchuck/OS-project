--- newlib-3.1.0/newlib/libc/machine/arm/memchr.S-orig	2020-07-28 16:15:29.411099818 -0500
+++ newlib-3.1.0/newlib/libc/machine/arm/memchr.S	2020-07-28 16:16:26.545396708 -0500
@@ -79,7 +79,11 @@
 
 @ NOTE: This ifdef MUST match the one in memchr-stub.c
 #if defined (__ARM_NEON__) || defined (__ARM_NEON)
+#if __ARM_ARCH_PROFILE == 'R'
+	.arch	armv8-r
+#else
 	.arch	armv7-a
+#endif
 	.fpu	neon
 
 
